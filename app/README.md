# Optional UI (week 6 only)

Streamlit or FastAPI+React view over the human-review queue: submission,
diagnosis with highlighted evidence spans, routing reason, and the draft
feedback. Read-only against `results/*.jsonl`.

This is not the point of the project and must not consume time from the eval.
